# Randomized PCA

function rpca(X::Array{T,2},
              k::Int,
              center::Bool=true,
              retx::Bool=true,
              p::Int=10,
              q::Int=2) where {T<:Number}

    m, n = size(X)

end
