# Randomized interpolative decomposition

function rid(X,
             k::Int,
             mode,
             p::Int=10,
             q::Int=0,
             idx_only::Bool=false,
             random::Bool=true)

    m, n = size(X)
end
